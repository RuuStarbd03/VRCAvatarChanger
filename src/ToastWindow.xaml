<Window x:Class="VRCAvatarChanger.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{StaticResource {x:Type Window}}"
        Title="VRCAvatarChanger" SizeToContent="WidthAndHeight"
        WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ShowActivated="False" Focusable="False" IsHitTestVisible="False">
    <Border x:Name="Root" Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
            CornerRadius="{StaticResource CardRadius}" Padding="16,10" MaxWidth="420" Opacity="0">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="3" Opacity="0.4" Color="#000000"/>
        </Border.Effect>
        <StackPanel Orientation="Horizontal">
            <TextBlock x:Name="ToastIcon" Style="{StaticResource Icon}" FontSize="13" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <TextBlock x:Name="Message" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
        </StackPanel>
    </Border>
</Window>
